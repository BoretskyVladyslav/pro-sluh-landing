# Дизайн-система ProSluh

Еталон для лендингу сурдології. Джерело правди для кольору, типу, юридичних формулювань і аналітики.

## Колірна палітра (`@theme`)

Токени оголошені в `src/styles/global.css`. У розмітці — лише утиліти Tailwind (`bg-brand-600`, `text-cta-700`), без довільних HEX.

| Токен | HEX | Призначення |
|---|---|---|
| `brand-50` | `#F1F7F8` | Фон секцій, чіпи |
| `brand-100` | `#D6EAEE` | Бордери, вторинний фон |
| `brand-600` | `#0B5D6B` | Вторинні кнопки, посилання, акцент |
| `brand-900` | `#0E3A46` | Заголовки, логотип |
| `cream` | `#FAF7F2` | Фон сторінки |
| `ink-900` | `#14262B` | Основний текст |
| `ink-600` | `#4A5B61` | Підзаголовки, дисклеймери |
| `cta-700` | `#C2410C` | **Лише цільова дія** |
| `cta-800` | `#9A3412` | Hover CTA |
| `vet-50` | `#F3EFE6` | Фон ветеранського бейджа/блока |
| `vet-300` | `#C8B08A` | Бордер ветеранського акценту |
| `vet-900` | `#26332B` | Текст ветеранського акценту |
| `ok` / `err` / `warn` | `#15803D` / `#B91C1C` / `#B45309` | Стани форм |

### Правило CTA

Помаранчевий (`cta-700` / `cta-800`) — **виключно** для конверсійної дії: «Записатися», «Зателефонувати» як головний дзвінок з реклами. Не використовувати для навігації, чіпів, бейджів, посилань у тексті чи декора. Вторинні дії — `brand-600` або контур `btn-outline`.

## Типографіка

| Роль | Сімейство | Клас |
|---|---|---|
| Текст, UI | Inter Variable, system-ui | `font-sans` |
| Заголовки H1–H4, логотип | Manrope Variable | `font-display` |

- Базовий кегль: **18px** (`text-base` = `1.125rem`) — зручність для пацієнтів 60+.
- Інтерактив: **min-height 56px** (`h-14` / `min-h-14`), tap-зона не менше **48px** (`min-w-12`).
- Класи кнопок: `.btn` + `.btn-cta` | `.btn-brand` | `.btn-outline`.
- Ієрархія: один `H1` на сторінку; далі `H2` секцій, без пропусків рівнів.

## Юридичні обмеження (ст. 21 ЗУ «Про рекламу»)

Обов'язковий дисклеймер у футері (і за потреби біля офер):

> Самолікування може бути шкідливим для вашого здоров'я. Перед застосуванням обов'язкова консультація лікаря.

**Заборонено в копі та візуалі:**

- Слова й обіцянки: «гарантую», «вилікую», «назавжди», «100% результат», чудо-ефект без доказової бази.
- Фото, портрет або 3D-зображення **лікаря в рекламному блоці Hero**.
- Порівняння «кращий за всіх», відгуки без можливості перевірки, якщо вони виглядають як медична гарантія.

**Дозволено:** факти послуг (аудіометрія, підбір апарата), гео (Дніпро), ім'я спеціаліста текстом поза рекламним портретом, цінові умови програми для ветеранів із зірочкою та посиланням на умови.

Hero: лише кабінет, обладнання або схематична аудіометрія (SVG/WebP/AVIF з `width` і `height`). Лікар — окремий інформаційний блок (`#likar`), не LCP-реклама.

## Схема подій `data-evt`

Усі кліки конверсії розмічаються атрибутами без клієнтського JS на старті (збирач читає DOM / делегування пізніше).

| `data-evt` | `data-loc` | Коли |
|---|---|---|
| `booking_click` | `hero` \| `sticky` \| `footer` | «Записатися» / «Записатися онлайн» → `#zapis` |
| `click_call` | `hero` \| `header` \| `sticky` | `tel:+380…` |
| `veteran_click` | `hero` \| `veterany` | Перехід до програми для ветеранів `#veterany` |

Обов'язкові атрибути на цільовому `<a>` / `<button>`:

```html
<a href="#zapis" data-evt="booking_click" data-loc="hero">Записатися онлайн</a>
```

Нові екрани наслідують ту саму пару `data-evt` + `data-loc`. Не вигадувати синоніми (`cta_click`, `phone_click`).
